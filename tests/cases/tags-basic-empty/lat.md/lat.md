# Tags Basic Empty

Same shape as tags-basic, but the document carries no tags. Used as the
"un-federated" baseline for the byte-identical-output end-to-end test.

- [[feature]] — Feature module docs
