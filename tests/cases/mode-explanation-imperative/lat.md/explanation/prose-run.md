---
lat:
  mode: explanation
---
# Same Sentences As Ordinary Prose

The two sentences below are byte-for-byte the ones that topic.md keeps inside
its frontmatter and its fenced blocks.

Run the setup once before anything else.

Run the server before continuing.
